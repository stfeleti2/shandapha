# Branch protection

Protect the default branch with:

- required status checks for lint, typecheck, test, and build
- pull request review before merge
- no force-pushes
- linear history or merge queue if the team size later justifies it

This keeps the modular monolith safe without heavyweight process.
