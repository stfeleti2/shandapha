# GitHub environments

Recommended environment names:

- `preview` for pull request validation
- `staging` for release candidates
- `production` for customer traffic

Keep required secrets minimal and prefer environment protection rules over
custom deployment scripts.
