# Railway deployment notes

Railway is a good default for early hosted deployments when the goal is
shipping speed over infra customization. Keep the services split by deployable
unit, not by imagined future microservices.
