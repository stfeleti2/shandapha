{
  "name": "@shandapha/registry-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser-safe registry client and local-first catalog helpers.",
  "sideEffects": false,
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --project tsconfig.json",
    "lint": "biome check --config-path ../../../configs/biome.json src",
    "test": "vitest run"
  },
  "dependencies": {
    "@shandapha/contracts": "workspace:*",
    "@shandapha/packs": "workspace:*",
    "@shandapha/templates": "workspace:*"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
