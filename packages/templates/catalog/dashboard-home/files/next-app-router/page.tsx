export default function DashboardHomePage() {
  return (
    <main data-template="dashboard-home">
      <h1>Dashboard Home</h1>
      <p>
        Overview shell with KPI rails, chart cards, activity cards, and action
        slots.
      </p>
    </main>
  );
}
