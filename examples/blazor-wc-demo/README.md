# blazor-wc-demo

Reference notes for Blazor consuming Shandapha through Web Components.

The generator should output:

- script import for the WC bundle
- theme CSS import
- Razor snippets using custom element tags
- notes for handling `CustomEvent` payloads in Blazor components

This keeps Blazor supported without maintaining a separate component
implementation.
