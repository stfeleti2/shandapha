# wc-vanilla

Reference notes for the universal Web Components baseline.

This example is the portability proof:

- import the generated component bundle
- import the theme CSS
- render WC tags in a plain HTML or server-rendered page
- listen for `CustomEvent`-based interaction contracts

If another framework is not a first-class adapter yet, this example is the
fallback integration surface.
