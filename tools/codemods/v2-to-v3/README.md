# v2 to v3 codemods

Use this directory for later major-version migrations once the API, token
contract, or template manifest shape evolves. Keep transforms deterministic so
the generator can safely reference them during upgrades.
